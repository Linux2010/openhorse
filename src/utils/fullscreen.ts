/**
 * openhorse - Fullscreen utility stub
 */

export function isMouseClicksDisabled(): boolean {
  // In openhorse, mouse click handling is always enabled when mouse tracking is active.
  // Fullscreen-only gating is not needed.
  return false;
}
